# fsn-error

`FsyError` (thiserror-based), `Repairable` trait, and `ValidationIssue` for structured error handling.

## Usage

```rust
use fsn_error::{FsyError, Repairable, ValidationIssue};
// See src/lib.rs for full API details.
```
