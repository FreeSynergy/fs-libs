# fsn-health

`HealthLevel`, `HealthStatus`, `HealthIssue`, `HealthRules`, and the `HealthCheck` trait.

## Usage

```rust
use fsn_health::{HealthCheck, HealthStatus, HealthLevel};
// See src/lib.rs for full API details.
```
