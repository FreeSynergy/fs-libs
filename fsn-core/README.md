# fsn-core

`FormAction` and `SelectionResult` — shared UI primitives with zero dependencies.

## Usage

```rust
use fsn_core::{FormAction, SelectionResult};
// See src/lib.rs for full API details.
```
