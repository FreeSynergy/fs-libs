# fsn-i18n

Snippet-based i18n: `t()`, `t_with()`, and `load_dir()` for loading translation files at runtime.

## Usage

```rust
use fsn_i18n::{t, t_with, load_dir};
// See src/lib.rs for full API details.
```
