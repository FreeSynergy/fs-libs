# fs-auth

JWT token handling and RBAC permission checks for FreeSynergy services.

## Features

| Feature | Default | Description             |
|---------|---------|-------------------------|
| `jwt`   | no      | JWT encode/decode via jsonwebtoken |

## Usage

```rust
use fs_auth::JwtAuth;
// See src/lib.rs for full API details.
```
